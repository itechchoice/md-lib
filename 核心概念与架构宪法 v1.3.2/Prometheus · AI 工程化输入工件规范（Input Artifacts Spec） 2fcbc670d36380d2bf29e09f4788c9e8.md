# Prometheus · AI 工程化输入工件规范（Input Artifacts Spec）

[返回 🔙](https://www.notion.so/Prometheus-2fcbc670d36380228079f7d55ad487b8?pvs=21)

---

<aside>
💡

## **我们不控制 Cursor，我们控制输入空间。**

> 本页定义了一套**将 AI 代码生成纳入工程体系的输入工件规范**
> 

> 通过冻结架构宪法、时序图、状态机、Plane 级 SPEC、禁止清单、Prompt 模板、基准输出与生成记录，将 Cursor 的代码生成过程从一次性对话，转化为
> 
> 
> **对一组可版本控制、可审计、可复现输入工件的确定性翻译过程**
> 

> 在 Prometheus 架构下，**代码不再是源代码，输入工件才是系统 DNA**
> 
</aside>

---

一套**可直接落地、可版本控制的 Input Artifacts 清单**，最后一个**推荐的 Repo 结构**，让 Cursor 的生成过程变成**可复现、可演进、可审计的工程行为**。

---

## **一句话结论（定锚）**

> **要把 Cursor 的不确定性降到最低，
就必须把“生成”从一次对话，
变成“对一组冻结输入工件（artifacts）的确定性翻译”。**
> 

换句话说：

> **我们不是在“让 Cursor 写代码”，
而是在“用 Cursor 编译一套输入规范”。**
> 

---

## **一、你真正要控制的不是 Cursor，而是「输入空间」**

Cursor 的不确定性主要来自三件事：

1. **输入不完整**（它会补）
2. **输入有歧义**（它会猜）
3. **输入不可追溯**（你无法复现）

所以正确做法是：

> **把所有“允许它知道的东西”显性化、冻结、版本化。**
> 

---

## **二、完整 Input Artifacts 清单（这是重点）**

下面这 **8 类输入工件**，**全部都需要纳入版本控制**。

这些不是“文档”，而是**生成系统的“源代码”**。

---

### **1️⃣ Architecture Constitution（宪法）**

📄 architecture/constitution.md

**内容**

- 四/五个 Plane 的职责定义
- Authority 分配
- 不可做之事（MUST NOT）
- 失败语义（FAILED 是终态）

**作用**

- Cursor 的**最高约束**
- 所有 Prompt 的“不可覆盖前提”

> 这是整个生成体系的 **root of trust**。
> 

---

### **2️⃣ Frozen Sequence Diagrams（时序冻结）**

📄 architecture/sequence/*.md

（内含 Mermaid）

**内容**

- 正常执行
- 异步执行
- 审批
- 失败 / 终态
- 多轮 Intent

**作用**

- 冻结**因果方向**
- 防止 Cursor 发明新路径（retry / loop / shortcut）

> **时序比接口更重要。**
> 

---

### **3️⃣ State Machines（状态冻结）**

📄 architecture/state-machines/*.md

**内容**

- Execution 状态枚举
- 审批状态
- 允许 / 禁止的迁移

**作用**

- 防止 Cursor 引入新状态
- 防止“看起来很合理”的 PAUSED / RETRYING

---

### **4️⃣ Plane SPEC（字段级冻结）**

📄 spec/interaction-surface.yaml

📄 spec/governance.yaml

📄 spec/decision.yaml

📄 spec/reconcile.yaml

📄 spec/execution.yaml

**内容**

- API 输入 / 输出
- EB / EP / ExecutionState 的字段定义
- 事件结构

**作用**

- 把“架构”变成**schema**
- Cursor 只能填充，不敢发挥

> 这是你们最重要的一层 artifact。
> 

---

### **5️⃣ Explicit Non-Goals（禁止清单）**

📄 architecture/non-goals.md

**内容（示例）**

```
- No retry in execution
- No execution-time branching
- No dynamic replan
- No shared state across planes
- No workflow engine
```

**作用**

- Cursor 对 “NOT” 的遵从度 > “应该做什么”
- 极大减少“好心补逻辑”

---

### **6️⃣ Plane Prompt Template（Prompt 本身也是工件）**

📄 prompts/decision.prompt.md

📄 prompts/reconcile.prompt.md …

**内容**

- 固定前缀（宪法 + 非目标）
- 固定任务描述
- 固定输出格式

**作用**

- Prompt = **生成编译器**
- 可以 diff、review、rollback

> 这是我**们真正的“AI 源码”**
> 

---

### **7️⃣ Golden Output（基准生成结果）**

📄 golden/decision-plane/v1/

📄 golden/reconcile-plane/v1/

**内容**

- 一次“正确生成”的代码快照
- 不一定是最终代码

**作用**

- 回归测试
- 不同语言实现的对照参考
- 判断 Cursor 是否“开始走歪”

---

### **8️⃣ Generation Manifest（生成记录）**

📄 generation/run-2024-xx.yaml

**内容**

```yaml
cursor_version: x.y
model: gpt-4.1 / claude
input_artifacts:
  - constitution.md@sha
  - decision.yaml@sha
  - decision.prompt.md@sha
output:
  repo: prometheus-decision-plane
  commit: abcdef
```

**作用**

- 可追溯
- 可复现
- 可对比

> 这是把 AI 生成纳入工程审计的关键一步。
> 

---

## **三、推荐的 Repo 结构（可直接用）**

```
prometheus/
├── architecture/
│   ├── constitution.md
│   ├── non-goals.md
│   ├── sequence/
│   └── state-machines/
├── spec/
│   ├── interaction-surface.yaml
│   ├── governance.yaml
│   ├── decision.yaml
│   ├── reconcile.yaml
│   └── execution.yaml
├── prompts/
│   ├── interaction.prompt.md
│   ├── decision.prompt.md
│   ├── reconcile.prompt.md
│   └── execution.prompt.md
├── golden/
│   └── decision-plane/v1/
├── generation/
│   └── run-*.yaml
└── README.md
```

---

## **四、这样做之后，你会获得什么能力？**

### **✅ 可重复生成**

- 同一组 artifacts → 同一类代码结构

### **✅ 跨语言生成**

- 换语言 ≠ 换架构
- 只换 Prompt 的 implementation 部分

### **✅ 可控演进**

- 改 SPEC → 重新生成
- diff 的是 **输入，不是输出**

### **✅ 护城河增强**

- 别人抄代码 ≠ 抄 artifacts
- **artifacts 才是“系统 DNA”**

---

## **五、最重要的一句话（可以写进 README）**

> **在 Prometheus 中，代码不是源代码，输入工件才是。**
> 

---

## **如果你愿意，下一步我可以继续帮你：**

1. 🧱 **直接帮你生成一套 spec/*.yaml 的初始模板**
2. 🤖 **把你现有的 Prompt 全部整理成“可版本化 Prompt 工件”**
3. 🧪 **设计一套“Cursor 生成回归测试”机制**

你选一个，我就直接往下做。

<aside>
💡

[git@github.com](mailto:git@github.com):itechchoice/prometheus-dna-artifacts.git
[https://github.com/itechchoice/prometheus-dna-artifacts](https://github.com/itechchoice/prometheus-dna-artifacts)

</aside>

---

## Artifacts

[**GPT 生成 artifacts 的最小必需输入集（Core Input Set）**](GPT%20%E7%94%9F%E6%88%90%20artifacts%20%E7%9A%84%E6%9C%80%E5%B0%8F%E5%BF%85%E9%9C%80%E8%BE%93%E5%85%A5%E9%9B%86%EF%BC%88Core%20Input%20Set%EF%BC%89%202fcbc670d3638085a8b7c57044bc047b.md)